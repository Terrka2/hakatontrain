# Статус B0 · ИИ-оператор: самостоятельный проход

Владелец: Женёк · Ветка: `feat/B0-operator-l0` → `pair/backend`
Файл обновляют: человек (копирует доску планировщика) или CLI-агент после каждой закрытой задачи. Формат — как выдаёт планировщик, руками не изобретать.

## Доска B0 · уровень L0
| # | Задача | Статус | Проверка |
|---|--------|--------|----------|
| 1 | Написать тесты на критерии приёмки | ⏳ | 19 тестов B0 зелёные; полный QA и проверки всех критериев на реальных портах ещё не завершены |
| 2 | Связать extract/verify, clusters, context и priority через публичные порты | ✅ | Реальный fixture: 13 кластеров, первые два содержат r001/r011, factors заполнены, r006 требует проверки |
| 3 | Реализовать manual-проход, черновик плана и ленту в памяти | ✅ | Реальный operator_run возвращает plan_id; модульные тесты draft, порядка, повторяемости и копий состояния |
| 4 | Обработать ошибки B4/B6 без падения прохода | ✅ | Ошибки score/make_jobs/solve → decisions, plan_id=None, кластеры остаются доступны |
| 5 | Добавить чтение кластеров, фильтры и supervisor-review | ✅ | API-тесты списка, деталей, фильтров, accept/reject и 401/403/404/422 |
| 6 | Добавить ручной запуск и чтение ленты | ✅ | API-тесты manual, limit и порядка; L0 использует фиксированное время fixture |
| 7 | Пройти полный QA и подготовить изменения для приёмки | ❌ | Общий прогон: 85 passed / 1 failed; мутации, полный QA и живой HTTP-прогон ещё не выполнены |
Блокеры: extractor дополнительно помечает r005 как suspicious; B6 исключает его и даёт 8 задач вместо expect.jobs_after_batching=9. Работа остановлена по просьбе пользователя, открывается draft для ревью.
Обновлено: 2026-09-23 00:35

## QA L0
Промежуточный результат; уровень НЕ принят.

- Команда из backend: `python -m pytest tests/blocks/operator tests/blocks/clusters tests/blocks/extractor tests/blocks/dispatch tests/blocks/ingest tests/blocks/geo tests/blocks/priority tests/blocks/context -q -p no:cacheprovider --tb=short`.
- Результат: **85 passed, 1 failed**. Падает `tests/blocks/dispatch/test_dispatch.py::test_batching_and_route_constraints`: `len(jobs) == 9`, фактически 8.
- У B0 **19 passed**: 5 проверок без подмен бизнес-портов + 14 модульных/API проверок с явно выделенными doubles. Тест наличия модели не считается сквозной проверкой. API-тесты используют FastAPI TestClient и подмену пользователя; живой сервер и настоящий вход ещё не проверены.
- Реальный pipeline: 13 кластеров; верхние два — r001 и r011; r006 имеет needs_review; ручной запуск возвращает plan_id. Порты соседей реально вызываются. В продуктовой реализации нет тестовых doubles, таблиц ожидаемых результатов или чтения fixture.expect.
- Причина 8 задач: список review = cl_r005, cl_r006. У r005 срабатывают три правила extractor; оба кластера исключаются диспетчером. Группа cl_r007–cl_r010 собрана правильно, service_min=144. Следующий шаг — согласовать/исправить взаимодействие правил проверки с эталоном; fixture.expect не менять.
- Проверки B3: 13 кластеров, три DupLink у cl_r001, разделение категорий и отдельных ям, исключение resolved, детерминированность, окно 30 дней, similarity, пустой ввод и повторные id.
- Проверки extractor: признаки r011, suspicious r006, confirmed r001, текст-инструкция не даёт confirmed, регистр/optional поля. Это L0 без LLM; L1-провайдер не реализован.
- Проверки B6: сроки, короткие смены, replan с сохранением done, перенос навыка, not_found, storm прошли; проверка числа задач падает до последующих assertions в том же тесте, поэтому их нельзя считать выполненными этим прогоном.
- Ruff check новых/изменённых файлов B0/B3/B6/extractor и их тестов — All checks passed. Форматирование выполнено. Общий линтер всего репозитория и полная suite с PostgreSQL в этой сессии не запускались.
- Конфигурация для импорта тестов задавалась фиктивными PROJECT_NAME/SECRET_KEY/DATABASE_URL/FIRST_SUPERUSER/FIRST_SUPERUSER_PASSWORD. B0-тесты блокируют socket.create_connection; Windows socket.connect не блокируется, поскольку нужен внутреннему asyncio socketpair. HTTP через TestClient не ходит в сеть.
- Не выполнены: полный мутационный QA, все граничные сценарии, типизация проекта, отдельная проверка L1 fallback, полный живой HTTP/авторизация, Docker rebuild текущей интеграции, окончательный аудит размера/путей отдельных PR.

## Отчёт L0
БЛОК: B0 · УРОВЕНЬ: L0, промежуточный draft, не готов к merge.

ВЕТКИ: исходная B0 — feat/B0-operator-l0; общие модели — codex/B0-contract-models; воспроизводимая цепочка с зависимостями — codex/B0-integration-l0. База pair/backend: ac41ca0. main/dev/pair/backend не менялись; ветки коллег не переписывались.

РАЗРЕШЕНИЯ: пользователь сообщил о разрешении Арсения на необходимые изменения для B0. После уточнения отдельно разрешил недостающие L0 соседних блоков и подробный отчёт. Поэтому изменение models.py и расширенный набор зависимостей явные, а не случайный выход за B0.

ЧТО СДЕЛАНО В B0:
- Общие модели ClusterOut (Cluster + вложенный priority), ClusterDetail (плюс reports), ClusterReview и OperatorRunRequest добавлены в канонический models.py. При отказе scoring priority=null, выдуманный балл не подставляется.
- run_pipeline: извлечение признаков всех обращений, verification, кластеризация, контекст, приоритеты; входные Report копируются; проверяются повторные id и timezone now.
- operator_run: fixture → pipeline → make_jobs → solve/replan → только draft → журнал; ошибки B4/B6 сохраняют кластеры и дают plan_id=None. Состояние в памяти защищено RLock; наружу возвращаются копии.
- Кластеры: сортировка по убыванию priority, category/min_score, детали с обращениями; supervisor принимает или отклоняет спорное. Accept снимает needs_review при следующем проходе, reject исключает кластер из планирования.
- Лента: новые сверху с limit; ручной запуск supervisor. GET также требует авторизации. Локальные API возвращают 503 при незавершённых соседних портах.
- Демо-время берётся из fixture.now, чтобы возраст обращений и смены не зависели от даты на компьютере.

ПОДКЛЮЧЁННЫЕ РАБОТЫ КОЛЛЕГ (cherry-pick, авторство сохранено):
| Блок | Исходный коммит | Содержание |
|---|---|---|
| B1 | 4ced2e8 | L0 загрузка/парсер/роуты и тесты |
| B2 | 50086ea + 596b98f | L0 геометрия и исправление within |
| B5 | 9833365 | L0 контекст и погодные правила |
| B4 | 0241138 | L0 HZ/DM/AG/FL и тесты |
Конфликт factors/ex.py разрешён сохранением существующей реализации X1 из pair/backend; её не заменяли заглушкой B4. Более поздние L1/L2 из веток коллег не подключались; их review-fixes также не перенесены автоматически и требуют отдельной оценки.

ДОПОЛНИТЕЛЬНО РЕАЛИЗОВАНО:
- B3: union-find, расстояния B2, Jaccard L0, детерминированные cl_<id>, DupLink.
- L1-extractor, только уровень L0: словари RU/RO, правила suspicious/confirmed; нет обращений к LLM.
- B6: группировка выездов, greedy 30 км/ч, навыки/смены/deadline, baseline, replan; GET /plan/draft и /plan/{id} возвращают план B0. Полный B6 ещё НЕ завершён: создание/approve/current/crews API и полный QA не реализованы.
- B5: небольшой адаптер распознавания cl_<report_id> в погодных правилах; без него storm не распознавал категорию кластера.

ИЗМЕНЁННЫЕ ПУТИ: backend/app/contracts/models.py; backend/app/blocks/operator/**; backend/app/api/routes/{clusters,operator}.py; backend/tests/blocks/operator/**; docs/status/B0.md. Дополнения зависимостей — пакеты clusters/extractor/dispatch, plan.py, их tests/status; одна строка context/l0.py. Подключённые коммиты B1/B2/B4/B5 перечислены отдельно выше.

КРИТЕРИИ ПРИЁМКИ B0: 1–2 подтверждены реальным pipeline, но GET проверен с doubles; 3 — реальный plan_id, draft отдельно в unit; 4 — pipeline без подмен, маршруты отдельно в unit; 5 — unit с отказами B4/B6; 6 — API unit. Полный сквозной API-набор без подмен ещё нужен. Не отмечать L0 принятым.

РАЗМЕР И ГРАНИЦЫ PR: интеграционная ветка содержит несколько блоков и больше 200 строк без тестов. Draft служит обзором текущего состояния; **не сливать его целиком**. Перед merge разделить на общий контракт, B0 orchestration/API, B3, extractor L0, B6 и B5 fix; существующие B1/B2/B4/B5 принимать через PR их авторов. check_paths B0 на общей ветке закономерно не зелёный. Политики CI/allowlist для обхода не менялись. В docs/team отметки приёмки не ставились.

ПРОДОЛЖЕНИЕ: разобраться с лишним review r005 → реальный HTTP прогон и accept r006 → полный QA B0 → разделение веток/PR по блокам и размеру → ревью Арсения/Некита. Код сегодня больше не дорабатывается по просьбе пользователя.
