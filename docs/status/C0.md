# Статус C0 · Каркас проекта

Владелец: Женёк · Ветка: `feat/C0-<кратко>` → `dev`
Файл обновляют: человек (копирует доску планировщика) или CLI-агент после каждой закрытой задачи. Формат — как выдаёт планировщик, руками не изобретать.

## Доска C0 · уровень L0
| # | Задача | Статус | Проверка |
|---|--------|--------|----------|
| 1 | Добавить проверки запуска, fixture и авторизации без сети | ✅ | Первоначально 3 failed / 3 passed; после исправлений 6 passed |
| 2 | Подключить миграции и демо-пользователей при запуске Compose | ✅ | Чистый Docker-том: все миграции применены, prestart завершился с кодом 0, демо-пользователи входят |
| 3 | Исправить меню по ролям и проверить его в браузере | ✅ | Playwright C0: 10 passed, роли supervisor/crew/citizen/unknown/admin, 390/1440 px |
| 4 | Уточнить команды и адреса запуска в README | ✅ | Сервис mailpit, frontend внутри backend :8000, prestart перед API |
| 5 | Прогнать тесты, линтер и сборку каркаса | ✅ | pytest: 32 passed; Ruff check/format, Biome, tsc, build — успешно |
| 6 | Проверить стек Docker, PostgreSQL и письма Mailpit | ✅ | 82 passed на PostgreSQL; check_c0_stack.py — 1 passed, реальное письмо и сброс пароля |
| 7 | Завершить оставшиеся пункты C0-QA и согласовать машинный список путей | ☐ | Мутационная QA, применимые UI-пункты, check_paths перед PR |
Блокеры: compose.yml отсутствует в машинной маске путей C0, требуется согласование владельцев контрактов перед PR. Блокер Docker снят.
Обновлено: 2026-09-22 18:40

## QA L0
Предварительная проверка C0 · уровень L0 · 2026-09-22 18:26

- Критерии 1–3: проверены сборка и запуск на новом Docker-томе, все миграции, /docs, health-check, регистрация, логин и сброс пароля. Backend, PostgreSQL 18 и Mailpit healthy. Реальное письмо доставлено в Mailpit, ссылка ведёт на тестовый frontend, новый пароль работает, старый отклоняется.
- Критерий 4: 14 существующих тестов импортируют порты всех блоков; бизнес-логика блоков не менялась.
- Критерий 5: проверены идемпотентность seed офлайн, роли и логин четырёх демо-пользователей через живой API с PostgreSQL, включая /users/me.
- Fixture: модели и данные не менялись; frontend-копия совпадает с backend.
- Первичный запуск новых тестов: 3 failed / 3 passed. Обнаружены отсутствие prestart, ошибка кодировки почтового шаблона и несовместимость SQLite с передачей строкового UUID в Session.get. Последнее не исправлялось в замороженном deps.py: офлайн-проверка seed читает роли из тестовой БД; /users/me успешно проверен с PostgreSQL.
- Итог backend: `pytest tests/blocks tests/test_fixture_valid.py tests/db -q` — 32 passed. Для импорта приложения заданы локальные тестовые PROJECT_NAME, SECRET_KEY, DATABASE_URL, FIRST_SUPERUSER и FIRST_SUPERUSER_PASSWORD; реальных ключей нет.
- Полный backend внутри Docker: `python -m pytest -p no:cacheprovider tests -q --tb=short` — 82 passed, 49 warnings (демо-секреты из .env.example). Тесты используют отдельную c0_tests, демо-база app сохранена. Исправлены адреса mock send_email в двух существующих тестах, чтобы они не обращались к внешнему SMTP.
- Интеграционный HTTP/SMTP-тест: `pytest tests/check_c0_stack.py -q` — 1 passed; запускается явно, не входит в обычный офлайн-набор.
- Стек: проект citytriage-c0, файлы compose.yml + compose.override.yml + backend/tests/compose.c0.yml, --env-file .env.example. Сайт/API: http://localhost:18000; Mailpit: http://localhost:18025; PostgreSQL: 127.0.0.1:15432. Установленный PostgreSQL Windows на 5432 не изменялся. Контейнеры оставлены работающими.
- Первое Windows-подключение к PostgreSQL задержалось; дублирующий Windows-прогон остановлен, итоговый полный набор выполнен внутри контейнера.
- Ruff: `check backend/app backend/tests` — All checks passed; `format --check` — 88 files already formatted.
- Frontend: `tsc --noEmit`, `bun run build`, Biome без автоисправления общего дерева — успешно.
- Браузер: `node ../node_modules/@playwright/test/cli.js test --config playwright.c0.config.ts` — 10 passed. Edge, API подменён, 390/1440 px, роли меню и отсутствие горизонтального скролла/pageerror. Предупреждения dev-сервера о FORCE_COLOR и react-swc остаются; консоль браузера отдельно не проверялась.
- Полная мутационная QA и остальные пункты C0-QA пока не закрыты. L1 fallback и границы бизнес-портов к реализации каркаса L0 не относятся. Размеры всех кнопок и состояния будущих экранов не принимаются этими тестами.
- Границы: compose.yml и замороженный AppSidebar.tsx изменены с явного разрешения Женька для C0. Контракты и paths.json не менялись; автоматический allowlist пока не разрешает compose.yml. Проверка check_paths сравнивает коммиты, поэтому до коммита не проверяет эти рабочие изменения.

## Отчёт L0
БЛОК: C0 · УРОВЕНЬ: L0 (не завершён)

ВЕТКА: feat/C0-skeleton-l0 → PR в dev по контракту C0. Ветка создана от origin/dev, поскольку локальная origin/pair/backend ещё указывает на начальную структуру без каркаса.

ИЗМЕНЁННЫЕ ФАЙЛЫ: compose.yml; README.md; backend/app/utils.py; backend/tests/blocks/test_skeleton.py; backend/tests/blocks/test_skeleton_auth.py; backend/tests/check_c0_stack.py; backend/tests/compose.c0.yml; backend/tests/api/routes/test_login.py; backend/tests/api/routes/test_users.py; frontend/src/components/Sidebar/AppSidebar.tsx; frontend/playwright.c0.config.ts; frontend/tests/c0/sidebar.spec.ts; docs/status/C0.md.

ТЕСТЫ: 82 backend на PostgreSQL + 1 интеграционный HTTP/SMTP + 10 браузерных — зелёные; добавлено 6 backend + 1 интеграционный + 10 браузерных. Линтеры, типы и сборка прошли.

КРИТЕРИИ ПРИЁМКИ: [x] 1 — стек поднят на новом томе; [x] 2 — 82 теста; [x] 3 — реальный Mailpit и сброс пароля; [x] 4 — порты присутствуют; [x] 5 — демо-пользователи входят с правильными ролями.

QA: предварительная, полная приёмка не закрыта. Отметка ✅ в docs/team/Женёк.md не ставилась.

ВЫШЕЛ ЗА РАЗРЕШЁННЫЕ ПУТИ: да — compose.yml с разрешения пользователя; AppSidebar.tsx разморожен тем же разрешением только для C0. Остальные изменения в путях C0, чужая бизнес-логика и CI не затронуты.

ВОПРОСЫ / БЛОКЕРЫ: оставшиеся пункты C0-QA и машинный allowlist compose.yml. Docker, PostgreSQL и Mailpit проверены. Изменения подготовлены для draft PR в dev по запросу Женька; публикация для ревью не означает приёмку уровня. К L1 не переходили.
