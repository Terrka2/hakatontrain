# Статус C0 · Каркас проекта

Владелец: Женёк · Ветка: `feat/C0-<кратко>` → `dev`
Файл обновляют: человек (копирует доску планировщика) или CLI-агент после каждой закрытой задачи. Формат — как выдаёт планировщик, руками не изобретать.

## Доска C0 · уровень L0
| # | Задача | Статус | Проверка |
|---|--------|--------|----------|
| 1 | Добавить проверки запуска, fixture и авторизации без сети | ✅ | 32 passed; SMTP/TCP в auth-тестах заблокированы |
| 2 | Подключить миграции и демо-пользователей при запуске Compose | ✅ | Чистый том проверен ранее; повторный prestart завершился с кодом 0 |
| 3 | Исправить меню по ролям и проверить его в браузере | ✅ | 14 passed: 5 ролей, 390/1440 px, состояния сессии, мобильные размеры |
| 4 | Уточнить команды и адреса запуска в README | ✅ | mailpit, frontend внутри backend :8000, prestart перед API |
| 5 | Прогнать тесты, линтер и сборку каркаса | ✅ | 32 offline; Ruff check/format, Biome, tsc, production build |
| 6 | Проверить стек Docker, PostgreSQL и письма Mailpit | ✅ | 82 passed на PostgreSQL; 1 passed HTTP/SMTP с реальным письмом |
| 7 | Завершить оставшиеся пункты C0-QA и согласовать машинный список путей | ✅ | 3/3 мутации обнаружены; compose*.yml разрешён изменениями Арсения из dev; границы QA ниже |
Блокеры: технических блокеров C0 L0 не найдено; PR #3 ожидает ревью и merge в dev.
Обновлено: 2026-09-22 23:49

## QA L0
QA C0 · уровень L0 · 2026-09-22

1. ✅ Все 5 критериев приёмки: запуск/prestart — test_startup_initializes_database_before_backend и Docker; /docs/health — test_docs_health_and_optional_routes; auth/письмо — test_signup_login_and_password_recovery и check_c0_stack.py; гнёзда — существующие тесты портов; seed — test_demo_accounts_login_with_fixture_crew_ids и живой /users/me четырёх демо-пользователей.
2. ✅ 3/3 мутации обнаружены: пустой почтовый шаблон, обратный порядок seed/миграций, исключение рендера письма. Команда из backend: `python -m tests.check_c0_mutations`. Все подмены только в памяти и восстановлены; затем обычные тесты зелёные. Исключение проверено в C0-рендерере, не в L1: L1-порта у каркаса нет.
3. N/A для бизнес-порта: C0 предоставляет структуру проекта. Повторный seed не создаёт дубликаты, optional crew_id=None проверен у руководителя. Даты, списки обращений и нормализация строк — критерии соседних блоков, здесь не реализуются.
4. N/A: у C0 нет runtime-переключателя L0/L1; L1 этого контракта — CI. Fallback бизнес-портов не проверялся и не реализовывался.
5. ✅ Детерминированность seed: повторная инициализация сохраняет набор id и роли; канонические fixture совпадают.
6. ✅ check_paths C0; актуальный dev объединён merge-коммитом e483fed. Модели, fixture, manifests/locks, сгенерированный клиент и остальные замороженные файлы не менялись. Исключение AppSidebar.tsx ранее явно разрешил Женёк. Контракты изменил Арсений в dev, не этот PR.
7. ✅ В изменённом коде C0 нет print/TODO/FIXME/XXX; Ruff чистый. Пустые функции соседних блоков сохранены по контракту.
8. ✅ Офлайн-набор: 32 passed. В auth-тестах socket.create_connection запрещён, SMTP подменён. При импорте приложения нужны фиктивные настройки окружения шаблона; реальных ключей нет. Полный PostgreSQL-набор и HTTP/SMTP проверяются отдельно, они не выдаются за офлайн.
9. ✅ Размер PR без тестов меньше 200 добавленных/удалённых строк относительно origin/dev; в подсчёт включены документация и Playwright config.
10. ✅ Ruff check: All checks passed; format --check: 90 files already formatted.
11. ✅ Auth/API шаблона: 401 без токена, 403 чужие права, 404 отсутствующий пользователь/отключённые маршруты, 422 пустое тело регистрации. Новых бизнес-роутов в C0 нет.
12. ✅ Diff backend/app/fixtures пуст; expect не менялся; frontend fixture совпадает с backend.
13. ✅ Biome: 78 files, no fixes; tsc --noEmit и tsc build прошли; Vite production build прошёл. Линтер запускался без --write/--unsafe, чтобы не переформатировать чужие файлы.
14. ✅ В пределах каркаса: на главной заглушке проверены pending/null/401 для currentUser; при 401 очищается токен и открывается login. Полные состояния будущих экранов F1–F5 и их mock/real-порты не принимаются этим аудитом.
15. ✅ В пределах навигации C0: 5 ролей, 390/1440 px, нет горизонтального скролла; мобильный переключатель и пункты меню ≥48 px. Исправлены исходные 28/32 px. Полный визуальный аудит содержимого будущих экранов относится к их владельцам.
16. ✅ В frontend/src/features отсутствуют литеральные hex/rgb-цвета.
17. ✅ При проверке меню всех ролей браузерные pageerror, console.error и console.warning отсутствуют. Ожидаемый HTTP 401 проверяется отдельно. В терминале Vite остаются предупреждения FORCE_COLOR и рекомендация сменить react-swc; зависимости по правилам не менялись.

Найдено и починено: prestart перед API, UTF-8 почтовых шаблонов, меню по ролям, реальные пути mock send_email, размеры мобильной навигации. Дополнены тесты консоли и состояний сессии; ошибка повторного внесения токена при навигации была в тесте и исправлена.
Не починено: дефектов реализации C0, обнаруженных текущими проверками, не осталось. Пункты 3/4 неприменимы к бизнес-порту; ограничения 14/15 описаны выше и не означают приёмку чужих экранов.
Добавлено тестов относительно dev: 6 backend + 1 явный HTTP/SMTP + 14 Playwright; отдельно 3 мутационных эксперимента.

## Отчёт L0
БЛОК: C0 · УРОВЕНЬ: L0 — готов к ревью; merge и приёмку подтверждает ревьюер.

ВЕТКА: feat/C0-skeleton-l0 → dev. PR: https://github.com/Terrka2/hakatontrain/pull/3

ИЗМЕНЁННЫЕ ФАЙЛЫ: compose.yml; README.md; backend/app/utils.py; backend/tests/api/routes/test_login.py; backend/tests/api/routes/test_users.py; backend/tests/blocks/test_skeleton.py; backend/tests/blocks/test_skeleton_auth.py; backend/tests/check_c0_stack.py; backend/tests/check_c0_mutations.py; backend/tests/compose.c0.yml; frontend/src/components/Sidebar/AppSidebar.tsx; frontend/src/routes/_layout.tsx; frontend/playwright.c0.config.ts; frontend/tests/c0/sidebar.spec.ts; docs/status/C0.md.

ТЕСТЫ: 82 passed на PostgreSQL (49 предупреждений о демо-секретах); 32 passed offline; 1 passed HTTP/SMTP; 14 passed Playwright; 3/3 мутации обнаружены. Финальная проверка 422 добавлена после полного PostgreSQL-прогона и прошла в повторном offline-наборе. Ruff/Biome/tsc/build зелёные.

КРИТЕРИИ ПРИЁМКИ: [x] 1 — запуск и /docs; [x] 2 — тесты; [x] 3 — auth и реальное письмо Mailpit; [x] 4 — гнёзда портов; [x] 5 — демо-роли.

QA: 15/15 применимых пунктов ✅ с указанными границами; 2 N/A (3, 4). Отметка приёмки в docs/team/Женёк.md оставлена ревьюеру: PR ещё draft/open, не merged. К L1 или B0 не переходили.

ВЫШЕЛ ЗА РАЗРЕШЁННЫЕ ПУТИ: нет по актуальному allowlist. AppSidebar.tsx изменён по ранее данному явному разрешению пользователя на исключение из заморозки.

ВОПРОСЫ / БЛОКЕРЫ: требуется ревью Арсения или Некита и merge PR #3. Тестовый стек citytriage-c0 запущен: API http://localhost:18000, Mailpit http://localhost:18025, PostgreSQL 127.0.0.1:15432; Windows PostgreSQL на 5432 не изменялся. При повторном аудите стек был остановлен, после запуска сервисов проверки прошли. Для проверки используются только db/mailpit/prestart/backend; необязательный сервис playwright из общего override без собственного образа не запускался.
