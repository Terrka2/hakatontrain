# Статус D2 · Эмбеддинги и семантический поиск (RAG)

Владелец: Некит · Ветка: `feat/D2-<кратко>` → `pair/llm`
Файл обновляют: человек (копирует доску планировщика) или CLI-агент после каждой закрытой задачи. Формат — как выдаёт планировщик, руками не изобретать.

## Доска D2 · уровень L0
| # | Задача | Статус | Проверка |
|---|--------|--------|----------|
| 1 | Тесты на критерии приёмки (красные) | ✅ | `uv run pytest tests/blocks/search -v` |
| 2 | TF-IDF порт: `Hit`/`embed`/`text_similarity`/`search` (l0.py) | ✅ | `uv run pytest tests/blocks/search -v` |
| 3 | Роут `GET /api/v1/search` с защитой по роли (`require_roles`) | ✅ | `uv run pytest tests/blocks/search -k route -v` |
| 4 | Откат на L0 при ошибке/таймауте L1 (monkeypatch) | ✅ | `uv run pytest tests/blocks/search -k l1 -v` |
| 5 | Детерминированность и граничные случаи (пусто/None/дубликаты) | ✅ | `uv run pytest tests/blocks/search -v` |
Блокеры: — регистрация роута в `backend/app/api/main.py` уже сделана C0 заранее (см. «Отчёт»)
Обновлено: 2026-09-23 17:18

## QA L0
QA D2 · уровень L0 · 2026-09-23 17:18
Пункты: 1 ✅ · 2 ✅ (3/3 мутации покраснели) · 3 ✅ · 4 ✅ · 5 ✅ · 6 ✅ · 7 ✅ · 8 ✅ · 9 ✅ (200 строк без тестов) · 10 ✅ · 11 ✅ · 12 ✅ · 13 N/A · 14 N/A

Пункты 13–14 (модель как данные / невалидный ответ LLM) не применимы: D2 не вызывает LLM-провайдера — это TF-IDF/эмбеддинг-блок (`backend/app/llm` не используется).

Найдено и починено:
- п.2 (мутации): (1) `text_similarity` возвращает константу 0.5 → тест `test_text_similarity_identical_strings_is_one` покраснел; (2) знак сортировки в `search` инвертирован → 3 теста покраснели (`test_search_pothole_near_school_returns_r001_or_r003`, `test_search_race_returns_event_e1`, `test_route_ok_for_crew_role_and_matches_response_model`); (3) в порту `__init__.py` убран `try/except` вокруг `l1.search` (исключение летит наружу) → `test_l1_model_unavailable_falls_back_to_l0_silently` покраснел. Все три мутации возвращены обратно.
- п.9: рефакторинг — вынесена общая функция ранжирования `l0.rank()`, переиспользуется в `l1.search()`, чтобы уложиться в лимит 200 строк без тестов.
- п.10 (mypy, вне обязательного списка QA): найдены `Literal["report","event"]`-ошибки в `Hit(kind=...)` — исправлены типизацией `_Doc.kind: Literal["report","event"]`. Остались отчёты mypy об отсутствии стабов для `sklearn`/`sentence_transformers` (`import-untyped`/`import-not-found`) — это на уровне зависимостей без py.typed, не баг блока; `pyproject.toml` менять нельзя.

Не починено: нет (в рамках путей блока).
Добавлено тестов: 23 (backend/tests/blocks/search/test_search.py)

## Отчёт L0
```
БЛОК: D2 · УРОВЕНЬ: L0
ВЕТКА: dev (работа велась напрямую в git worktree на dev, без отдельной feat-ветки — по указанию заказчика задачи)
ИЗМЕНЁННЫЕ ФАЙЛЫ:
  - backend/app/blocks/search/__init__.py (порт: embed/text_similarity/search с переключателем EMBEDDER, откат L1→L0)
  - backend/app/blocks/search/l0.py (новый: TF-IDF по символьным n-граммам, char_wb (1,3), индекс в памяти на fixture)
  - backend/app/blocks/search/l1.py (новый: sentence-transformers paraphrase-multilingual-MiniLM-L12-v2, ошибки не ловятся — их ловит порт)
  - backend/app/api/routes/search.py (роут GET /search, response_model=list[Hit], require_roles("supervisor","crew"))
  - backend/tests/blocks/search/test_search.py (новый: 23 теста)
  - docs/status/D2.md (эта доска)
ТЕСТЫ: `uv run pytest tests/blocks/search -v` → 23 passed. Полный набор `uv run pytest -q` (backend) → 99 passed, регрессий нет.
КРИТЕРИИ ПРИЁМКИ:
  [x] 1 — не проверяем как жёсткий критерий на L0: в контракте явно помечен «L1:» (sentence-transformers). На L0 (символьные TF-IDF n-граммы) такое упорядочивание RU/RO не гарантируется — тест `test_text_similarity_l1_ordering_is_l1_only` фиксирует это явно, а не подгоняет фиктивно зелёный тест под слабую реализацию.
  [x] 2 — `search("яма у школы")` возвращает r001 или r003 в топ-3 (проверено, top-3 = r016, r007, r001 при char_wb(1,3)).
  [x] 3 — `search("забег")` возвращает событие e1 в топ-3.
  [x] 4 — модель L1 недоступна/падает → тихий откат на tfidf без исключения наружу, warning в лог (monkeypatch на `l1.search`/`l1.embed`/`l1.text_similarity`).
QA: 12/12 применимых пунктов ✅ (13–14 N/A, не LLM-блок) · не починено: нет · добавлено тестов: 23
ВЫШЕЛ ЗА РАЗРЕШЁННЫЕ ПУТИ: нет
ВОПРОСЫ / БЛОКЕРЫ:
  - Роут уже зарегистрирован в `backend/app/api/main.py` (строка `api_router.include_router(search.router)`, файл заморожен для D2) — сам файл main.py я не трогал, только создал `backend/app/api/routes/search.py`, которого раньше не было. Работа блока C0 в этой части уже выполнена, дополнительных действий от владельца C0 не требуется.
  - Роль роута (`require_roles("supervisor", "crew")`) выбрана самостоятельно: в контракте D2 роль явно не указана (только «Роут: GET /api/v1/search?q=&k= → list[Hit]»), а из потребителей блока (B3, L2, L3) это внутренний инструмент для персонала, не для citizen. Владельцу контракта стоит подтвердить или скорректировать список ролей.
```
