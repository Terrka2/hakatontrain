# Статус F2 · Очередь приоритетов и карточка проблемы

Владелец: Лео · Ветка: `feat/F2-queue` → `pair/frontend`
Файл обновляют: человек (копирует доску планировщика) или CLI-агент после каждой закрытой задачи. Формат — как выдаёт планировщик, руками не изобретать.

## Доска F2 · уровень L0
| # | Задача | Статус | Проверка |
|---|--------|--------|----------|
| 1 | Написать тесты на критерии 2 и 3 (сумма сегментов = балл, r006 с needs_review и причинами), красные | ✅ | `node --test src/features/queue/bar.test.ts` |
| 2 | Сделать features/queue/api.ts (mock ↔ `/api/v1/clusters?sort=priority`, `/api/v1/reports`, `POST /clusters/{id}/review`) и хуки useQueue / useReports | ✅ | `npx tsc --noEmit` |
| 3 | Вынести логику PriorityBar в bar.ts (сегменты, ширины, остаток округления) и нарисовать PriorityBar | ✅ | тест критерия 2 |
| 4 | Сделать PriorityQueue: строка = кнопка ≥ 56 px, номер, иконка категории, адрес, компактный бейдж | ✅ | скриншоты 390/1440 |
| 5 | Сделать ClusterCard: бейджи, «требует проверки» с причинами и кнопками Принять/Отклонить, PriorityBar, обращения (язык, фото, подтверждения), «почему склеено» | ✅ | скриншоты, тест критерия 3 |
| 6 | Собрать QueueScreen: карта + очередь справа (≥768) / нижняя панель (390), выбор через useUiState, карточка плавающей панелью над картой | ✅ | скриншоты: клик по строке → карточка и подлёт карты; клик по маркеру → строка выделена |
| 7 | Подключить экран в routes/_layout/queue.tsx вместо заглушки C0 | ✅ | `bun run build` |
| 8 | Состояния loading / error / empty, роль: кнопки ревью только supervisor | ✅ | `npx tsc --noEmit` |
Блокеры: —
Обновлено: 2026-09-22 19:40

## QA L0
QA F2 · уровень L0 · 2026-09-22 19:40
Пункты: 1 ✅ (крит. 1 — Playwright: клик по строке → карточка, маркер aria-pressed, flyTo; клик по маркеру → строка aria-current; крит. 2, 3 — node-тесты; крит. 4 — скриншоты 390 px, hscroll=false) · 2 ✅ (3/3: константные ширины, обратный порядок, без остатка округления — тесты краснеют) · 3 ✅ (пустой список факторов, score=0, фактор без данных, кластер без адреса) · 4 — (уровня L1 нет) · 5 ✅ · 6 ✅ (`check_paths.py F2 --base feat/F1-map`) · 7 ✅ · 8 ✅ (node --test без сети; в браузере только ERR_CONNECTION_REFUSED к :8000 от useAuth каркаса) · 9 ❌ (≈430 строк без тестов при лимите 200) · 10 ✅ · 11 ✅ (Skeleton / ErrorState с «Повторить» / EmptyState) · 12 ✅ (390 px без горизонтального скролла, строки ≥ 56 px, кнопки ревью 48 px) · 13 ✅ · 14 ✅
Найдено и починено: сумма подписей расходилась с баллом на 0.1 из-за округления каждого фактора на сервере — остаток показывается на самом крупном сегменте; ширина полосы могла превысить 100 % — ограничена; на десктопе карточка заменяла очередь и строка не подсвечивалась — карточка стала плавающей панелью над картой; на телефоне карточка открывалась прокрученной — контейнер пересоздаётся по id кластера.
Не починено: 9 — один PR на L0 экрана: api + PriorityBar + карточка + экран. Делить на два (bar+card / screen) — решение владельца.
Добавлено тестов: 5

## Отчёт L0
БЛОК: F2 · УРОВЕНЬ: L0
ВЕТКА: feat/F2-queue  →  PR в: pair/frontend (после F6 L0 и F1 L0)
ИЗМЕНЁННЫЕ ФАЙЛЫ: frontend/src/features/queue/{api,bar,useQueue,index}.ts, frontend/src/features/queue/{PriorityBar,PriorityQueue,ClusterCard,QueueScreen}.tsx, frontend/src/features/queue/bar.test.ts, frontend/src/routes/_layout/queue.tsx, docs/status/F2.md, docs/team/Лео.md
ТЕСТЫ: node --test → pass 5, fail 0; tsc --noEmit чисто; bun run lint чисто; bun run build ✓
КРИТЕРИИ ПРИЁМКИ: [x] 1  [x] 2  [x] 3  [x] 4
QA: 13/13 применимых пунктов, 1 ❌ (размер PR) · не починено: 9 · добавлено тестов: 5
ВЫШЕЛ ЗА РАЗРЕШЁННЫЕ ПУТИ: нет
ВОПРОСЫ / БЛОКЕРЫ: (1) Кнопки «Принять / Отклонить» на mock меняют только локальное состояние карточки; `POST /clusters/{id}/review` появится с B0 — тогда после ответа нужно инвалидировать `["clusters"]`. (2) Типы ClusterOut/Factor/Report — из frontend/src/lib/contracts.ts (F1), после генерации клиента заменить на `@/client`. (3) Выбор кластера общий с картой через lib/ui-state — фильтры по категории/статусу (L1) туда же.
