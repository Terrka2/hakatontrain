# Статус F3 · Пульт руководителя: лента оператора, план, утверждение

Владелец: Арсений · Ветка: `feat/F3-dispatch` → `pair/frontend`
Файл обновляют: человек (копирует доску планировщика) или CLI-агент после каждой закрытой задачи. Формат — как выдаёт планировщик, руками не изобретать.

## Доска F3 · уровень L0
| # | Задача | Статус | Проверка |
|---|--------|--------|----------|
| 1 | Написать тесты на сравнение планов, подпись «4 проблемы · 1 выезд», строку ленты (красные) | ✅ | `node --test src/features/dispatch/diff.test.ts` |
| 2 | Вынести чистую логику в `diff.ts`: planDiff, stopLabel, feedLine, подписи решений и триггеров | ✅ | тесты задачи 1 |
| 3 | Сделать `api.ts` и хуки TanStack Query: лента, черновик, утверждённый план, бригады; опрос 5 с; mock через `lib/mock-store` | ✅ | `npx tsc --noEmit` |
| 4 | Собрать компоненты порта: OperatorFeed, PlanView, PlanDiff, DecisionLog, PlanVsBaseline, ApproveBar | ✅ | `npx tsc --noEmit && bun run build` |
| 5 | Собрать DispatchScreen: карта с маршрутами, кнопки «Ясно / Шторм» и «Сброс демо», состояния loading / error / empty | ✅ | скриншоты 1440 и 390 |
| 6 | Подключить экран в `routes/_layout/plan.tsx` вместо заглушки C0 | ✅ | `bun run build` |
| 7 | Проверить сценарии: утверждение → «отправлен бригадам», шторм → черновик v2 и «отложено» в журнале, 390 px без горизонтального скролла | ✅ | Playwright-скриншоты, `hscroll=false` |
Блокеры: —
Обновлено: 2026-09-22 19:40

## QA L0
QA F3 · уровень L0 · 2026-09-22 19:40
Пункты: 1 ✅ (крит. 1 — скриншоты + тест «шторм убирает ямы»; крит. 2 — тест stopLabel + скриншот; крит. 3 — скриншот после «Утвердить», роль через `useAuth`; крит. 5 — скриншот после «Шторм»: черновик v2, три «отложено» с причиной) · 2 ✅ (3/3: planDiff с константой, обратное сравнение crew, сломанный stopLabel — тесты краснеют) · 3 ✅ (planDiff на null/одинаковых планах, пустые маршруты, план без unassigned, feedLine без plan_id) · 4 — (L1 нет; VITE_USE_MOCK=false ведёт на роуты B0/B6, откат в L0 на фронте контрактом не предусмотрен) · 5 ✅ · 6 ✅ (`check_paths.py F3 --base feat/F1-map`) · 7 ✅ · 8 ✅ (node --test без сети; в браузере ошибок консоли нет, кроме ERR_CONNECTION_REFUSED к :8000 от useAuth каркаса) · 9 ❌ (≈830 строк без тестов при лимите 200) · 10 ✅ · 11 ✅ (Skeleton / ErrorState с «Повторить» / EmptyState «Оператор ещё не собирал план») · 12 ✅ (390 px без горизонтального скролла после `min-w-0` у колонок; кнопки «Утвердить» 48 px, демо-кнопки 40 px) · 13 ✅ · 14 ✅
Найдено и починено: колонки бригад на 390 px растягивали страницу (grid-элементы без `min-w-0`); кнопка «Утвердить» была выключена, потому что без бэкенда `useAuth().user` — это HTML от vite dev-сервера, а не объект пользователя (теперь роль читается только если у объекта есть `role`); «на 0% больше» при равных числах заменено на нейтральную фразу.
Не починено: 9 — один PR на весь L0 пульта (6 компонентов порта + экран + api). Делить: (1) diff.ts + api + хуки, (2) компоненты, (3) экран — решение владельца. Критерий 4 (бригада «не могу» → запись в ленте и черновик v2 с PlanDiff) в mock идёт через `submitJobUpdate` из `lib/mock-store` и проверяется совместно с F5; в тестах стора не покрыт.
Добавлено тестов: 5

## Отчёт L0
БЛОК: F3 · УРОВЕНЬ: L0
ВЕТКА: feat/F3-dispatch  →  PR в: pair/frontend (после F1: стоит на общем mock-«городе» и слое маршрутов CityMap)
ИЗМЕНЁННЫЕ ФАЙЛЫ: frontend/src/features/dispatch/{diff,api,useDispatch,index}.ts, frontend/src/features/dispatch/{ApproveBar,DecisionLog,DispatchScreen,OperatorFeed,PlanDiff,PlanView,PlanVsBaseline}.tsx, frontend/src/features/dispatch/diff.test.ts, frontend/src/routes/_layout/plan.tsx, docs/status/F3.md, docs/team/Арсений.md
ТЕСТЫ: node --test → pass 5, fail 0; tsc --noEmit чисто; bun run lint чисто; bun run build ✓
КРИТЕРИИ ПРИЁМКИ: [x] 1  [x] 2  [x] 3  [ ] 4 — сценарий бригады, проверяется вместе с F5  [x] 5
QA: 13/13 применимых пунктов, 1 ❌ (размер PR) · не починено: 9 · добавлено тестов: 5
ВЫШЕЛ ЗА РАЗРЕШЁННЫЕ ПУТИ: нет
ВОПРОСЫ / БЛОКЕРЫ: (1) Линии маршрутов на карте не видны: `resolveCssColor` в `features/map/routes.ts` (F1) читает токен через canvas `fillStyle`, а современный Chromium возвращает строку `oklch(...)` как есть; maplibre её не парсит. Нужно брать цвет через `getImageData` после `fillRect` или хранить в токенах rgb-дубликаты. Критерий 1 «маршруты трёх бригад разными цветами» на карте до этого не виден, колонки бригад с цветами — видны. (2) `PlanView` принимает необязательный `clusters` сверх порта, чтобы показывать категорию и адрес остановки; без него показывает навык и id задания. (3) Сравнение «план vs по очереди» на шторме даёт равные числа — ожидаемо при 6 выездах.
