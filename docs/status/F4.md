# Статус F4 · Панель ассистента

Владелец: Арсений · Ветка: `feat/F4-<кратко>` → `pair/frontend`
Файл обновляют: человек (копирует доску планировщика) или CLI-агент после каждой закрытой задачи. Формат — как выдаёт планировщик, руками не изобретать.

## Доска F4 · уровень L0
| # | Задача | Статус | Проверка |
|---|--------|--------|----------|
| 1 | Написать тесты на критерии приёмки 1–4: запрос с ui_state, действия show_plan/focus_cluster, pending без confirm, ошибка в истории (красные) | ✅ | `node --test src/features/assistant/*.test.ts` |
| 2 | Редьюсер сессии `session.ts`: send / reply / fail / decide, `buildRequest` с копией ui_state | ✅ | тесты критериев 1, 3, 4 |
| 3 | Действия над интерфейсом `actions.ts`: focus_cluster, show_plan, show_run, show_crew, set_filter, лишние игнорируются | ✅ | тесты критерия 2 |
| 4 | Слой данных `api.ts`: mock через `askAssistant`/`confirmPending`, API `POST /assistant` и `/assistant/confirm` с таймаутом 8 с | ✅ | `npx tsc --noEmit` |
| 5 | `ConfirmCard`: карточка решения, «Подтвердить»/«Отмена» ≥ 48 px | ✅ | скриншоты 1440/390 |
| 6 | `AssistantDrawer`: плавающая кнопка, Sheet справа / снизу, история, подсказки, trace «Как я думал», поле ввода | ✅ | `bun run build`, скриншоты |
| 7 | Сессия и открытость панели в модульном сторе, чтобы переход show_plan не терял разговор | ✅ | скриншот mobile-dark-confirm: после перехода на /plan история на месте |
Блокеры: —
Обновлено: 2026-09-22 19:30

## QA L0
QA F4 · уровень L0 · 2026-09-22 19:30
Пункты: 1 ✅ (крит. 1 — session.test; крит. 2 — actions.test; крит. 3 — session.test + скриншот с ConfirmCard; крит. 4 — session.test, таймаут через AbortController) · 2 ✅ (3/3: пустой ui_state в buildRequest, инвертированный approve в decide, потеря истории при fail — тесты краснеют) · 3 ✅ (пустой список actions, payload без cluster_id, неизвестные kind, пустое сообщение не уходит) · 4 — (переключателя уровня у панели нет; ошибка API → сообщение в панели) · 5 ✅ · 6 ✅ (`check_paths.py F4 --base feat/F1-map`) · 7 ✅ · 8 ✅ (node --test без сети) · 9 ❌ (≈330 строк без тестов при лимите 200) · 10 ✅ · 11 ✅ (пусто: подсказка; ошибка: пузырь-alert; загрузка: «…») · 12 ✅ (390 px без горизонтального скролла, кнопка 128×48, «Подтвердить»/«Отмена» и «Отправить» 48 px) · 13 ✅ · 14 ✅ (в консоли только предупреждения WebGL headless-Chrome и ERR_CONNECTION_REFUSED к :8000 от useAuth каркаса)
Найдено и починено: панель, смонтированная в экране, размонтировалась при show_plan и теряла разговор — история и открытость перенесены в модульный стор (`store.ts`).
Не починено: 9 — один PR на панель целиком; делить на «логика + тесты» и «UI» — решение владельца.
Добавлено тестов: 7

## Отчёт L0
БЛОК: F4 · УРОВЕНЬ: L0 (+ подсказки, trace и ConfirmCard из L1 — на mock)
ВЕТКА: feat/F4-assistant  →  PR в: pair/frontend (после F1: использует lib/mock-store, lib/ui-state, lib/contracts)
ИЗМЕНЁННЫЕ ФАЙЛЫ: frontend/src/features/assistant/{index,session,actions,api,store}.ts, frontend/src/features/assistant/{AssistantDrawer,ConfirmCard}.tsx, frontend/src/features/assistant/{session,actions}.test.ts, docs/status/F4.md, docs/team/Арсений.md
ТЕСТЫ: node --test → pass 7, fail 0; tsc --noEmit чисто; biome чисто; bun run build ✓
КРИТЕРИИ ПРИЁМКИ: [x] 1  [x] 2  [x] 3  [x] 4
QA: 13/13 применимых пунктов, 1 ❌ (размер PR) · не починено: 9 · добавлено тестов: 7
ВЫШЕЛ ЗА РАЗРЕШЁННЫЕ ПУТИ: нет
ВОПРОСЫ / БЛОКЕРЫ: (1) `AssistantDrawer` нужно смонтировать в экранах F1 (карта) и F3 (план): `_layout.tsx` не в путях F4. Панель самодостаточна, состояние переживает переходы. (2) Роуты `POST /api/v1/assistant` и `/assistant/confirm` — контракт L2/L3 Некита; форма запроса `{pending_id, approve}` для confirm — моё предположение, согласовать. (3) Порт `applyUiActions(actions, navigate?)`: второй аргумент добавлен, без него переходы между экранами не выполняются.
