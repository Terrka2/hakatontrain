# Статус F5 · Экран бригады (телефон)

Владелец: Лео · Ветка: `feat/F5-crew` → `pair/frontend`
Файл обновляют: человек (копирует доску планировщика) или CLI-агент после каждой закрытой задачи. Формат — как выдаёт планировщик, руками не изобретать.

## Доска F5 · уровень L0
| # | Задача | Статус | Проверка |
|---|--------|--------|----------|
| 1 | Написать тесты на критерии 3 и 5 (без фото «Сделано» недоступно, причина/навык для «Не могу», баннер только при смене версии), красные | ✅ | `node --test src/features/crew/logic.test.ts` |
| 2 | Вынести чистую логику экрана в logic.ts (nextStop, canFinish, buildUpdate, routeChanged, geoLink, formatTime) | ✅ | тест критериев 3 и 5 |
| 3 | Сделать features/crew/api.ts (mock ↔ `GET /crew/me/route`, `POST /crew/jobs/{id}/status`) и хуки useMyRoute / useJobUpdate (опрос 10 с) | ✅ | `npx tsc --noEmit` |
| 4 | Сделать NextStopCard: категория, адрес, кол-во проблем, кнопка «Открыть в навигаторе» (geo:-ссылка) | ✅ | скриншот 390 px |
| 5 | Сделать StopActions (Приехал / Сделано с фото / Не могу) и FailDialog (причина, выбор навыка, заметка) | ✅ | скриншот, критерий 3 живьём |
| 6 | Собрать CrewRouteScreen: карта сверху, следующая остановка, действия, список остановок, баннер «Маршрут обновлён» | ✅ | скриншоты 390/1440 |
| 7 | Подключить экран в routes/_layout/crew.tsx вместо заглушки C0 | ✅ | `bun run build` |
| 8 | Состояния loading / error / «план не утверждён» (крит. 4); роль crew — только «Мой маршрут» в меню (крит. 1, код Sidebar заморожен F6) | ✅ | скриншот EmptyState, чтение AppSidebar.tsx |
Блокеры: —
Обновлено: 2026-09-22

## QA L0
QA F5 · уровень L0 · 2026-09-22
Пункты: 1 ✅ (крит. 1 — код: `AppSidebar.tsx` фильтрует пункты по `role==="crew"`, живой проход не смог проверить роль без бэкенда, см. блокер ниже; крит. 2 — Playwright 390 px, hScroll=false, все кнопки экрана ≥48 px; крит. 3 — node-тест + живой проход: «Сделано» disabled без фото, «Отправить оператору» disabled без причины; крит. 4 — скриншот EmptyState «План ещё не утверждён»; крит. 5 — node-тест routeChanged) · 2 ✅ (3/3 мутации покраснели: `canFinish` → константа `true`; `routeChanged` → `===` вместо `!==`; проверка фото в `buildUpdate` вырезана) · 3 ✅ (пустой список остановок → `nextStop` возвращает null; все остановки закрыты → null; фото/причина/навык отсутствуют → понятные ошибки; заметка с пробелами по краям — обрезается; `photo_url`/`reason`/`needs_skill`/`note` всегда `null` для несоответствующего статуса) · 4 — (уровня L1 нет, откат не применим) · 5 ✅ (детерминированные чистые функции, второй вызов на тех же данных даёт тот же результат и порядок) · 6 ✅ (`check_paths.py F5 --base feat/F1-map` зелёный после отката правок вне путей — см. «Найдено и починено») · 7 ✅ (`grep -rn "print(\|TODO\|FIXME\|XXX" frontend/src/features/crew` пусто) · 8 ✅ (`node --test` без сети; в браузере только предупреждения WebGL от maplibre, не от кода блока) · 9 ❌ (724 строки без тестов при лимите 200) · 10 ✅ (`bun run lint`, `tsc --noEmit`, `bun run build` чисты) · 11 ✅ (Skeleton / ErrorState с «Повторить» / EmptyState «План ещё не утверждён»; mock и API дают одну форму `MyRoute` из `api.ts`) · 12 ✅ (390 px без горизонтального скролла, кнопки ≥48 px, текст не обрезан; 1440 px — карточка в узкой колонке, не растянута) · 13 ✅ (`grep -rn "#[0-9a-fA-F]\{3,6\}\|rgb(" frontend/src/features/crew` пусто) · 14 ✅ (Playwright: console только WebGL-warning от карты, без ошибок приложения)
Найдено и починено: (1) незакоммиченные правки прошлого агента трогали `frontend/src/features/map/CityMap.tsx` и `frontend/src/features/map/routes.ts` — файлы вне «Разрешённых путей» F5 (дублировали фикс, который уже есть в более свежем `feat/F1-map`, ветка F5 просто отстала). Откатил их (`git checkout -- ...`), экран бригады использует уже существующий в базовой ревизии слой `kind: "crew"` в `CityMap`, ничего не сломалось. (2) `bun run lint` подправил форматирование одного файла автоматически.
Не починено: 9 — один PR на L0 экрана целиком (логика + api + хуки + 4 компонента + подключение роута), по образцу F2. Делить — решение владельца. 1(частично) — не удалось живьём проверить, что вход `crew1@demo.md` сразу открывает `/crew`: после логина `useAuth.ts` всегда делает `navigate({ to: "/" })`, а `/` — это `MapScreen` (F1) независимо от роли; фильтрация пунктов меню в `AppSidebar.tsx` по роли уже корректна и не требует правок. Правка редиректа лежит в `frontend/src/hooks/useAuth.ts` и/или `frontend/src/routes/_layout/index.tsx` — вне путей F5, нужен Арсений/Некит.
Добавлено тестов: 4 (в составе logic.test.ts, покрывают критерии 3 и 5 и вспомогательную логику)

## Отчёт L0
БЛОК: F5 · УРОВЕНЬ: L0
ВЕТКА: feat/F5-crew  →  PR в: pair/frontend (после F1 L0, вместе/после F2 и F6)
ИЗМЕНЁННЫЕ ФАЙЛЫ: frontend/src/features/crew/{logic,api,useMyRoute}.ts, frontend/src/features/crew/{CrewRouteScreen,NextStopCard,StopActions,FailDialog}.tsx, frontend/src/features/crew/logic.test.ts, frontend/src/features/crew/index.ts, frontend/src/routes/_layout/crew.tsx, docs/status/F5.md, docs/team/Лео.md
ТЕСТЫ: node --test src/features/crew/logic.test.ts → tests 4, pass 4, fail 0; npx tsc --noEmit чисто; npx bun run lint чисто; npx bun run build ✓ (chunk crew-*.js 13.08 kB)
КРИТЕРИИ ПРИЁМКИ: [x] 1 (код корректен; живая проверка роли ограничена отсутствием бэкенда) [x] 2 [x] 3 [x] 4 [x] 5
QA: 12/13 применимых пунктов ✅ · не починено: 9 (размер PR), частично 1 (редирект после логина — вне путей F5) · добавлено тестов: 4
ВЫШЕЛ ЗА РАЗРЕШЁННЫЕ ПУТИ: нет (откатил найденные у прошлого агента незакоммиченные правки в frontend/src/features/map/**, оставленные по ошибке)
ВОПРОСЫ / БЛОКЕРЫ: (1) После логина всегда `navigate("/")`, а корневой роут всегда показывает `MapScreen` — бригадир не попадает на `/crew` автоматически, только через пункт меню. Файлы вне путей F5 (`hooks/useAuth.ts`, `routes/_layout/index.tsx`) — нужен Арсений или Некит, чтобы завести редирект по роли. (2) `docs/status/F5.md` шаблон до этой правки указывал «Владелец: Арсений», хотя контракт и `paths.json` называют владельцем Лео (запасной — Арсений) — исправил на Лео по контракту, отметку L0 поставил в `docs/team/Лео.md`.
