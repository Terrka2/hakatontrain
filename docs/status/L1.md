# Статус L1 · LLM: извлечение признаков и проверка на нейрослоп

Владелец: Некит · Ветка: `feat/L1-<кратко>` → `pair/llm`
Файл обновляют: человек (копирует доску планировщика) или CLI-агент после каждой закрытой задачи. Формат — как выдаёт планировщик, руками не изобретать.

## Доска L1 · уровень L0
| # | Задача | Статус | Проверка |
|---|--------|--------|----------|
| 1 | Тесты на критерии приёмки (красные → зелёные) | ✅ | `cd backend && uv run pytest tests/blocks/extractor -v` |
| 2 | Реализовать `extract`/`verify` в `l0.py`: словари сигналов RU/RO, правила `verify` (≥3 сигнала → suspicious, подтверждения ≥2 → confirmed) | ✅ | `uv run pytest tests/blocks/extractor -k "criterion" -v` |
| 3 | Создать порт `backend/app/llm/__init__.py` (`complete_json`, сигнатура по контракту, без реального провайдера) | ✅ | `uv run pytest tests/blocks/extractor -k "criterion_5" -v` |
| 4 | Создать `l1.py` и переключатель `LLM=off|on` в `__init__.py` с тихим откатом на L0 при любой ошибке | ✅ | `uv run pytest tests/blocks/extractor -k "fallback" -v` |
| 5 | Тест на промпт-инъекцию в тексте обращения (не форсирует `confirmed`) | ✅ | `uv run pytest tests/blocks/extractor -k "injection" -v` |
| 6 | QA перед PR (весь чек-лист контракта) | ✅ | см. раздел QA ниже |
Блокеры: —
Обновлено: 2026-09-23 (агент, по задаче CLI)

## QA L0
```
QA L1 · уровень L0 · 2026-09-23
Пункты: 1 ✅ (5 тестов на 5 критериев, все зелёные) · 2 ✅ (3/3 мутации покраснели) · 3 ✅ (пустой список, 1 элемент, дубликаты id, None в address/photo_url, пробелы/регистр, границы дат с TZ) · 4 ✅ (откат L1→L0 через monkeypatch, warning в лог, наружу не летит) · 5 ✅ (два вызова подряд дают одинаковый результат и порядок reasons) · 6 ✅ (`python3 scripts/check_paths.py L1` зелёный, заморож. файлы не тронуты) · 7 ✅ (`grep print|TODO|FIXME|XXX` — пусто, типы у всех сигнатур) · 8 ✅ (сеть запрещена monkeypatch на urllib/socket, тесты зелёные) · 9 ✅ (178 строк без тестов: __init__.py 36 + l0.py 98 + l1.py 44) · 10 ✅ (`ruff check` и `ruff format --check` чисто) · 11 — н/п (роутов у блока L1 контракт не описывает, HTTP-роут не входит в разрешённые пути) · 12 ✅ (`git diff backend/app/fixtures/` пуст, раздел expect не менялся) · 13 ✅ (тест `test_model_response_as_data_prompt_injection_in_system_call`: инструкция в user-тексте не подменяет схему ответа) · 14 ✅ (`complete_json` вернул `None`/мусор → блок отдаёт результат L0 без исключения, тест `test_complete_json_invalid_response_falls_back`)
Найдено и починено: тест на регистр/пробелы (test_whitespace_and_case_insensitive_text) изначально был неверно сформулирован — переписан на текст, реально совпадающий с паттерном "открыт\w* люк"; линтер поймал неиспользуемую переменную и unused kwargs в тестах — исправлено.
Не починено: нет
Добавлено тестов: 21
```

## Отчёт L0
```
БЛОК: L1 · УРОВЕНЬ: L0
ВЕТКА: dev (по прямому указанию задачи — коммит сразу в dev в изолированном worktree, без отдельной feat/pair-ветки и PR)
ИЗМЕНЁННЫЕ ФАЙЛЫ:
  - backend/app/blocks/extractor/__init__.py (порт: переключатель LLM=off|on, тихий откат L1→L0)
  - backend/app/blocks/extractor/l0.py (новый: словари сигналов RU/RO, правила verify)
  - backend/app/blocks/extractor/l1.py (новый: вызов complete_json со схемами Extracted/Verification)
  - backend/tests/blocks/extractor/test_extractor.py (новый: 21 тест)
  - docs/status/L1.md (эта доска)
  backend/app/llm/__init__.py не менялся — уже содержал нужную сигнатуру complete_json (заглушка NotImplementedError), реальный провайдер не подключался (вне объёма L0).
ТЕСТЫ: `uv run pytest tests/blocks/extractor -v` → 21 passed, 3 warnings (предупреждения — про дефолтные секреты в .env, к блоку не относятся)
КРИТЕРИИ ПРИЁМКИ: [x] 1 (extract(r011) → injured=True и непустой hazard_signals)  [x] 2 (verify(r006, nearby=[]) → suspicious, reasons на русском)  [x] 3 (verify(r001, nearby=[r002,r003]) → confirmed, т.к. confirmations=5 ≥ 2)  [x] 4 (текст-инъекция не форсирует confirmed)  [x] 5 (LLM=on без ключа → результат L0 без исключения, LLM замокан)
QA: 13/14 пунктов ✅ (п. 11 «Роуты» — н/п, у блока L1 контракт не описывает HTTP-роуты) · не починено: нет · добавлено тестов: 21
ВЫШЕЛ ЗА РАЗРЕШЁННЫЕ ПУТИ: нет
ВОПРОСЫ / БЛОКЕРЫ: нет. Отклонение от референса origin/codex/B0-integration-l0: референсный l0.py не проверял self-исключение обращения из списка nearby в верификации граничного случая — добавлен явный тест и сохранено поведение (report.id != other.id уже было в референсе, просто добавлен тест-регресс). l1.py и переключатель LLM в __init__.py в референсной ветке отсутствовали (там порт всегда звал только l0) — дописаны по контракту (раздел «Уровни», L1: LLM=on использует complete_json, ошибка → откат на L0), т.к. это явно требуется контрактом и критерием приёмки 5.
```
