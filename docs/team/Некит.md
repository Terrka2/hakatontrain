# Некит: роль, ветка, блоки

> Скопируй этот файл в нейронку первым сообщением. Вторым — контракт текущего блока целиком.

## Роль в проекте
Данные и интеллект. База и репозиторий, эмбеддинги и поиск, всё, что зовёт LLM, инструменты оператора и MCP-сервер. Второй утверждающий merge.

## Ветка
Рабочая ветка: **`pair/llm`**. Под каждый блок и уровень: `git checkout pair/llm && git pull && git checkout -b feat/<ID>-<кратко>`. PR открываешь в `pair/llm`. В `dev` и `main` напрямую — нельзя.

## Мои блоки по порядку
| # | Блок | Что | Когда | Контракт | Доска | Референсы | L0 | L1 |
|---|---|---|---|---|---|---|---|---|
| 1 | `D1` | База данных и репозиторий | ядро | [контракт](../contracts/D1_db.md) | [доска](../status/D1.md) | [референсы](../references/D1/README.md) | ✅ | ☐ |
| 2 | `D2` | Эмбеддинги и семантический поиск (RAG) | ядро | [контракт](../contracts/D2_search.md) | [доска](../status/D2.md) | [референсы](../references/D2/README.md) | ☐ | ☐ |
| 3 | `L1` | LLM: извлечение признаков и проверка на нейрослоп | ядро | [контракт](../contracts/L1_extractor.md) | [доска](../status/L1.md) | [референсы](../references/L1/README.md) | ☐ | ☐ |
| 4 | `L2` | LLM: ассистент-диспетчер на фиксированном графе | ядро | [контракт](../contracts/L2_assistant.md) | [доска](../status/L2.md) | [референсы](../references/L2/README.md) | ☐ | ☐ |
| 5 | `L3` | Инструменты оператора и MCP-сервер | ядро | [контракт](../contracts/L3_tools.md) | [доска](../status/L3.md) | [референсы](../references/L3/README.md) | ☐ | ☐ |

Колонки L0 / L1 обновляет CLI-агент после приёмки уровня (`☐` → `✅`). Больше в этом файле ничего не менять.

## Мои обязанности по чужим блокам
- Запасной по блокам: C0, B0, B3, B5, B6, X1, F4. Если владелец выпал на 3 часа без PR — блок переходит ко мне.
- Первое ревью блоков: C0, B0, B3, B6, B7, F1, F2, F3, F4, F5, F7. Ревью ≤ 15 минут: критерии приёмки закрыты тестами · пути не нарушены · модели контрактов не скопированы · есть откат на L0.

## Цикл одной задачи
1. Контракт целиком → планировщику. Он выдаёт доску из 5–9 задач L0 и дальше задачи по одной.
2. Задачу → CLI-агенту. Смотри diff: файл вне «Разрешённых путей» контракта — откати.
3. Проверку из задачи запусти сам, вывод → планировщику: `готово N`.
4. Все ✅ → планировщик даёт `ЗАДАЧА <ID>-QA`, агент сам проходит QA из контракта и чинит найденное. Ты QA не делаешь. Потом агент пишет «Отчёт» в `docs/status/<ID>.md`, ты открываешь PR в `pair/llm` и кидаешь отчёт в чат.
5. Следующий уровень только после `L0 принят` от ревьюера. Пока ждёшь — L0 следующего своего блока.

## Куда идти с вопросами
- Не хватает поля в моделях или нужен файл вне своих путей → Арсений или Некит.
- Не понял контракт → владелец блока-соседа из таблицы в `docs/BLOCKS.md`, потом Арсений.
- Сломался `dev` → переключи блок на L0 переменной окружения, потом `git revert` merge-коммита.
